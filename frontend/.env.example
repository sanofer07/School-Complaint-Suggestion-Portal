# Frontend uses a CRA proxy to http://localhost:5000 (set in package.json)
# so no API URL env var is required for local development.
# If you deploy frontend and backend separately, set:
# REACT_APP_API_URL=https://your-backend-url.com/api
